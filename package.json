{
  "name": "grunt-local-socket",
  "description": "A socket.io simulated-server just for local development",
  "version": "0.0.1",
  "author": {
    "name": "danyfyang"
  },  
  "engines": {
    "node": ">= 0.8.0"
  },
  "scripts": {
    "test": "grunt localSocket"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "socket.io": "^1.4.8"
  }
}
